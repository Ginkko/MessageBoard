<h1>Question Detail:</h1>
<h2>{{title}}</h2>
<h4>Asked by: {{user}}</h4>
<p>{{text}}</p>


{{#if isEditing}}

{{else}}
  <button class="btn btn-danger" {{action "deleteQuestion"}}>Delete</button>
{{/if}}
